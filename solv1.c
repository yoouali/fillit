#include "fillit.h"

char	**ft_solv(char **map, char ***tab)
{
	t_pos		*pos;
	int			i;
	int			len;

	pos->r = 0;
	pos->c = 0;
	pos->a = 'A';
	i = 0;
	len = ft_strlen(map[0]);
	while (tab[i])
	{
		while ( t_pos->c < len && !check_pos(tab[i], map, t_pos))
	}
}
